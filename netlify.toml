[build]
  # 先進入 frontend 資料夾，然後執行 npm install，最後執行 npm run build
  command = "cd frontend && npm install && npm run build"
  publish = "frontend/dist"
  functions = "netlify/functions"

# 將 /api/* 的請求導向到我們的 functions
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200